# Profile photo

Drop your photo here as `me.jpg` (or `me.png`).

Recommended:
- 400 x 400 px or larger
- Square crop, face roughly centred
- Under 200 KB after compression

Until you upload it, the hero shows a purple-pink gradient circle with the letter "A" — the fallback automatically disappears the moment `me.jpg` exists.
